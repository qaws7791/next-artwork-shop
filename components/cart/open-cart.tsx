import { ShoppingCart } from "lucide-react";

export default function OpenCart() {
  return (
    <div className="border border-input bg-background hover:bg-accent hover:text-accent-foreground">
      <ShoppingCart />
    </div>
  );
}
